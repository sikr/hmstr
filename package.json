{
    "name": "hmstr",
    "version": "0.0.1",
    "description": "Obtain smarthome data via MQTT and forward it to Graphite Carbon Cache",
    "main": "app.js",
    "scripts": {
        "build": "npx tsc",
        "prebuild": "node src/scripts/setup.js ./",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "SiKr (simon.kruska@googlemail.com)",
    "license": "ISC",
    "dependencies": {
        "cron": "^2.4.4",
        "mqtt": "^4.3.8",
        "xml2js": "^0.6.2"
    },
    "devDependencies": {
        "@types/cron": "^2.4.0",
        "@types/node": "^18.19.26",
        "@types/ws": "^8.5.10",
        "@types/xml2js": "^0.4.14",
        "ts-node": "^10.9.2",
        "tsconfig-paths": "^4.2.0",
        "typescript": "^5.4.3"
    },
    "prettier": {
        "overrides": [
            {
                "files": "*.json",
                "options": {
                    "tabWidth": 4
                }
            }
        ]
    }
}
